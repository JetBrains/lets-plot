---

name: lets-plot-python-module
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - numpy>=1.24.2
  #- jupyter #=1.0.0
  - statsmodels # Required for residual plots 'lm' and 'loess' methods.
  - pandas>=1.5.3
  - geopandas>=0.12.2
  - shapely>=2.0.0
  - pytest>=8.1.1
  - jax >=0.3.25
  - pip
  - pip:
      - build #For Python wheel build.
      - pypng
      - palettable
      - polars==1.35.2 # Stable with Python tests in CI
      - pillow
