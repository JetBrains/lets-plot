---

name: lets-plot-python-module
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - numpy
  - jupyter
  - geopandas
  - pytest
  - pip
  - pip:
      - pypng
      - palettable
