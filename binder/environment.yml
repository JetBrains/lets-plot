---

name: ignored
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - numpy
  - pandas
  - matplotlib
  - pip
  - pip:                            # Packages installed from PyPI
      - lets-plot==1.4.0rc2
      - pypng
      - scikit-datasets
      - geopandas
      - requests
      - ipywidgets
      - sklearn
